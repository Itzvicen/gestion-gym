<div id="modal" class="hidden absolute inset-0 z-50 bg-gray-600 bg-opacity-50">
	<div class="flex items-center justify-center min-h-screen">
		<div class="md:h-auto 2xl:w-1/5 md:w-1/3 w-full bg-[#F9FAFB] p-6 rounded-lg">
			<form action="/dashboard/trainings/{{ training.getId }}/delete" method="POST">
        <p class="font-medium text-center">¿Estas seguro que quieres eliminar la clase <span class="font-bold">{{ training.getClassName }}</span>?</p>
				<div class="flex justify-center mt-4 gap-3">
					<button type="submit" class="w-full px-6 py-2 text-center text-white items-center justify-center inline-flex gap-1 leading-5 font-semibold rounded-md bg-red-600 hover:bg-red-700 focus:outline-none focus:bg-gray-600">
						Borrar
						<i class="fa-solid fa-trash"></i>
					</button>
					<button onclick="closeModal()" class="w-full close px-6 py-2 text-center items-center justify-center inline-flex leading-5 font-semibold rounded-md bg-black text-white" type="button">
						Cancelar
					</button>
				</div>
			</form>
		</div>
	</div>
</div>
