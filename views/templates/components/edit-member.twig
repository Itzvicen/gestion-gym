<div class="w-full md:p-8 p-4">
	<div class="mb-4">
		<h2 class="font-bold text-2xl">Informacion miembro</h2>
		<p>Visualiza y modifica los datos y pagos del miembro</p>
	</div>
	<div class="flex flex-col">
		<div class="w-full flex md:flex-row flex-col gap-6">
			<div class="md:h-auto md:w-[60%] w-full bg-[#F9FAFB] p-6 rounded-lg">
				<h3 class="font-bold text-lg">Editar</h3>
				<form class="flex flex-col" action="/dashboard/member/update/{{ member.getId }}" method="POST" enctype="multipart/form-data">
					<div class="md:flex mt-4 gap-x-6">
						<div class="md:w-1/3">
							<label class="text-gray-700" for="image">
								Imagen
							</label>
							<div class="flex items-center justify-center w-full mt-2">
								{% if member.getImagePath == 'uploads/pictures/default.png' %}
									<label for="dropzone-file" id="dropzone-container" class="flex flex-col items-center justify-center w-full h-[248px] border-2 border-gray-300 border-dashed rounded-lg cursor-pointer bg-gray-50  hover:bg-gray-100">
										<div class="flex flex-col items-center justify-center pt-5 pb-6">
											<svg aria-hidden="true" class="w-10 h-10 mb-3 text-gray-400" fill="none" stroke="currentColor" viewbox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
												<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12"></path>
											</svg>
											<p class="mb-2 text-sm text-gray-500 dark:text-gray-400 text-center">
												<span class="font-semibold">Click para subir</span>
												o arrastra<br/>
												y suelta</p>
											<p class="text-xs text-gray-500 dark:text-gray-400 text-center">PNG, JPG or GIF
												<br  />
												(MAX. 800x400px)</p>
										</div>
									</label>
									<input id="dropzone-file" class="hidden" type="file" name="image" onchange="loadFile(event)"/>
								{% endif %}
							</div>
							<img id="output" src="{% if member.getImagePath != 'uploads/pictures/default.png' %}../../{{ member.getImagePath }}{% endif %}" alt="Imagen de vista previa" class="w-full h-[248px] rounded-md object-cover {% if member.getImagePath == 'uploads/pictures/default.png' %}hidden{% endif %}"/>
							{% if member.getImagePath != 'uploads/pictures/default.png' %}
								<div id="dropzone-container" class="hidden"></div>
								<label for="dropzone-file" class="flex mt-5 flex-col items-center justify-center w-full h-[60px] border-2 border-gray-300 border-dashed rounded-lg cursor-pointer bg-gray-50  hover:bg-gray-100 ">
									<div class="flex items-center justify-center gap-2 pt-5 pb-4">
										<svg aria-hidden="true" class="w-8 h-8 mb-1 text-gray-400" fill="none" stroke="currentColor" viewbox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
											<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12"></path>
										</svg>
										<p class="text-xs text-gray-500 text-center">PNG, JPG or GIF
											<br>
											(MAX. 800x400px)</p>
									</div>
									<input id="dropzone-file" class="hidden" type="file" name="image" onchange="loadFile(event)"/>
								</label>
							{% endif %}
						</div>

						<div class="md:w-2/3 grid grid-cols-1 gap-6 sm:grid-cols-2 md:mt-0 mt-4">
							<div>
								<label class="text-gray-700" htmlfor="first_name">
									Nombre
								</label>
								<input class="block w-full px-4 py-2 mt-2 text-gray-700  bg-white border border-gray-200 rounded-md focus:border-blue-400 focus:ring-blue-300 focus:ring-opacity-40 focus:outline-none focus:ring" type="text" name="first_name" required value="{{ member.getFirstName }}"/>
							</div>
							<div>
								<label class="text-gray-700" htmlfor="last_name">
									Apellidos
								</label>
								<input class="block w-full px-4 py-2 mt-2 text-gray-700  bg-white border border-gray-200 rounded-md focus:border-blue-400 focus:ring-blue-300 focus:ring-opacity-40 focus:outline-none focus:ring" type="text" name="last_name" required value="{{ member.getLastName }}"/>
							</div>
							<div>
								<label class="text-gray-700" htmlfor="email">
									Email
								</label>
								<input class="block w-full px-4 py-2 mt-2 text-gray-700  bg-white border border-gray-200 rounded-md focus:border-blue-400 focus:ring-blue-300 focus:ring-opacity-40 focus:outline-none focus:ring" type="email" name="email" required value="{{ member.getEmail }}"/>
							</div>
							<div>
								<label class="text-gray-700" htmlfor="phone">
									Teléfono
								</label>
								<input class="block w-full px-4 py-2 mt-2 text-gray-700  bg-white border border-gray-200 rounded-md focus:border-blue-400 focus:ring-blue-300 focus:ring-opacity-40 focus:outline-none focus:ring" type="text" name="phone" required value="{{ member.getPhone }}"/>
							</div>
							<div>
								<label class="text-gray-700" htmlfor="registration_date">
									Fecha de registro
								</label>
								<input class="block w-full px-4 py-2 mt-2 text-gray-700  bg-white border border-gray-200 rounded-md focus:border-blue-400 focus:ring-blue-300 focus:ring-opacity-40 focus:outline-none focus:ring" type="date" name="registration_date" required value="{{ member.getRegistrationDate }}"/>
							</div>
							<div>
								<label class="text-gray-700" htmlfor="birth_date">
									Fecha de nacimiento
								</label>
								<input class="block w-full px-4 py-2 mt-2 text-gray-700  bg-white border border-gray-200 rounded-md focus:border-blue-400 focus:ring-blue-300 focus:ring-opacity-40 focus:outline-none focus:ring" type="date" name="birth_date" required value="{{ member.getBirthdate }}"/>
							</div>
							<div>
								<label class="text-gray-700" htmlfor="active">¿Está el usuario activo?</label>
								<select class="block w-full px-4 py-2 mt-2 text-gray-700  bg-white border border-gray-200 rounded-md focus:border-blue-400 focus:ring-blue-300 focus:ring-opacity-40 focus:outline-none focus:ring" name="active" required value="">
									<option value="1" {{member.getActive == 1 ? "selected" : "" }}>Si</option>
									<option value="0" {{member.getActive == 0 ? "selected" : "" }}>No</option>
								</select>
							</div>
						</div>
					</div>

					<div class="flex justify-end mt-8 gap-3">
						<button type="submit" class="px-6 py-2 text-center text-gray-100 items-center inline-flex gap-1 text-base leading-5 font-semibold rounded-md bg-black hover:bg-gray-600 focus:outline-none focus:bg-gray-600">
							Guardar
							<i class="fa-solid fa-download font-sm"></i>
						</button>
						<button type="button" onclick="openModal()" class="px-6 py-2 text-center text-white items-center inline-flex gap-1 text-base leading-5 font-semibold rounded-md bg-red-600 hover:bg-red-700 focus:outline-none focus:bg-gray-600">
							Borrar
							<i class="fa-solid fa-trash font-sm"></i>
						</button>
					</div>
				</form>
			</div>
			<div class="w-full md:w-[40%] bg-[#F9FAFB] p-6 rounded-lg">
				<h3 class="font-bold text-lg">Pagos</h3>
				<div class="flex flex-col gap-2 pt-4">
					{% if payments|length > 0 %}
						{% for payment in payments %}
							<table class="min-w-full divide-y-2 divide-gray-200 bg-white text-sm rounded-lg">
								<tbody class="divide-y divide-gray-200">
									<tr>
										<td class="whitespace-nowrap px-4 py-2 font-medium text-gray-900">
											{{ payment.getPaymentDate }}
										</td>
										<td class="hidden md:block whitespace-nowrap px-4 py-2 text-gray-700">{{ payment.getPaymentMethod }}</td>
										<td class="whitespace-nowrap px-4 py-2 text-gray-700">{{ payment.getAmount }}€</td>
										<td class="whitespace-nowrap px-4 py-2 text-gray-700">
											<p class="font-medium text-sm px-2 rounded-3xl text-center {{ payment.getPaymentStatus == 'Pagado' ? 'bg-green-200 text-green-500' : 'bg-red-200 text-red-500' }}">
												{{ payment.getPaymentStatus }}
											</p>
										</td>
									</tr>
								</tbody>
							</table>
						{% endfor %}
					{% else %}
						<p>Este miembro no tiene pagos registrados.</p>
					{% endif %}
				</div>
			</div>
		</div>

		<div class="mt-6 w-full bg-[#F9FAFB] p-6 rounded-lg">
			<h3 class="font-bold text-lg">Clases inscritas</h3>
			<div class="flex gap-2 pt-4 w-full">
				{% if classes|length > 0 %}
					{% for class in classes %}
						<a href="/dashboard/training/{{ class.getId }}" class="w-[25%] p-4 bg-white rounded-lg divide-y-2 divide-gray-200 text-sm hover:outline hover:outline-slate-300">
							<div class="py-2 font-medium text-gray-900">
								{{ class.getClassName }}
							</div>
							<div class="hidden md:block py-2 text-gray-700">{{ class.getLocation}}
								-
								{{ class.getClassTime }}</div>
							<div class="py-2 text-gray-700">{{ class.getInstructor }}</div>
						</a>
					{% endfor %}
				{% else %}
					<p>Este miembro no está inscrito en ninguna clase.</p>
				{% endif %}
			</div>
		</div>
	</div>
</div>
<script>
	{% if updateMessage %}
Toastify({
text: "{{ updateMessage }}",
duration: 3000,
newWindow: true,
close: true,
gravity: "top", // `top` o `bottom`
position: 'right', // `left`, `center` o `right`
stopOnFocus: true, // Detiene el cierre automático al poner el cursor sobre el toast
style: {
background: "rgb(187 247 208)",
color: "rgb(34 197 94)",
borderRadius: "10px"
}
}).showToast();{% endif %}
</script>
