<!DOCTYPE html>
<html lang="es">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>Dashboard</title>
		<link href="../dist/output.css" rel="stylesheet">
		<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" integrity="sha512-z3gLpd7yknf1YoNbCzqRKc4qyor8gaKU1qmn+CShxbuBusANI9QpRohGBreCFkKxLhei6S9CQXFEbbKuqLg0DA==" crossorigin="anonymous" referrerpolicy="no-referrer"/>
		<script type="text/javascript" src="https://cdn.jsdelivr.net/npm/toastify-js"></script>
		<script src="../src/js/dashboard.js" defer></script>
	</head>

	<body>
		<div class="flex antialiased">
			<div class="fixed z-20">
				{% include 'components/sidebar.twig' %}
			</div>
			<div class="flex flex-col w-full ml-[65px]">
				{% include 'components/header.twig' %}
				<div class="md:p-8 p-4">
					{% include 'components/stats.twig' %}
					{% include 'components/members.twig' %}
					{% include 'components/create_member.twig' %}
				</div>
			</div>
		</div>
		<script>
			{% if deleteMessage %}
				Toastify({
					text: "{{ deleteMessage }}",
					duration: 3000,
					newWindow: true,
					close: true,
					gravity: "top", // `top` o `bottom`
					position: 'right', // `left`, `center` o `right`
					stopOnFocus: true, // Detiene el cierre automático al poner el cursor sobre el toast
					style: {
					background: "rgb(187 247 208)",
					color: "rgb(34 197 94)",
					borderRadius: "10px"
					}
				}).showToast();
			{% endif %}
		</script>
	</body>
</html>